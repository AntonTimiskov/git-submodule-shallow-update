# git-submodule-shallow-update
A command line tool to make a git shallow copy of all submodules instead of real submodule update

# Requirements:
- Python 2.7

# Usage:
```
./git-submodule-shallow-update.py <path_to_git_reposigoty>
```
